# PLACEHOLDER — replace with a real age/SOPS-encrypted env file.
# Example of what a real file's first line looks like (age format):
#   age-encryption.org/v1
# Generate one with:
#   age --encrypt -R .age-recipients -o env/enc/production.env.enc env/dec/production.env
